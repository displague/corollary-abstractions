# v0.13 raw gate evidence

These are canonical-LF content copies of the accepted PowerShell 7 runner
receipts, logs, and exact module lists that produced `../test_gate_v013.json`.  They
were recovered from the host temporary directory on 2026-08-18.  Only the
Windows CRLF line encoding changed on retention.  The absolute `log` fields
are historical launch-time paths; the retained `logs/` files are their
content-preserving counterparts.  This directory does not revise or replace
the released aggregate ledger.

The corresponding module-list source names were `0`, `1split`, `2`, `3a`, and
`4`; the normalized retained names below follow the receipt shard numbers.
