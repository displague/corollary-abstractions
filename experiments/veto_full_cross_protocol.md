# The full cross-product control, registered before it runs (2026-08-20)

ROADMAP-v0.16 item 2, executed. Everything below is written before the
control reruns, and the run is exploratory by the item's own terms: the
population is a census with no fresh half, so nothing here is a
preregistered score — but the readout criterion is still fixed in
advance, because choosing a bar after seeing a distribution is the
practice this repository exists to end.

## The table

The incompatibility table is re-authored over the **full 26-kind
cross-product** (325 unordered pairs), blind: the author is an isolated
context (`claude -p`, empty non-git working directory, full tool
denylist, the same channel as the design courses) given ONLY the
26-entry kind menu with its glosses and the judging rules — not the
corpus, not the tags, not the slot inventory, not the v0.15 scoped
table, not any co-occurrence fact. Brief sha256_lf
`e74f86eae6b657f1454b07d3a99d4c59b7c4aa8582ba23cc0702fa50299bd7a8`
(394 words). The authored table is committed as
`experiments/veto_full_cross_table.json` with the session receipt.

## The rerun

`veto_score.py`'s corruption control is rerun unchanged except that the
kind-pair verdicts come from the full table (every permuted pair now has
a row, which is exactly what the v0.15 scoped table could not provide):
same tag-permutation scheme, same committed seed and permutation count
from `experiments/veto_prediction.json`, real tags scored once alongside.

## The readout criterion, fixed now

- **Established (exploratory):** the real-tags conflicting-slot count is
  strictly below the 5th percentile of the permuted distribution. That
  is the item's "control fails the shuffled tags", made numeric.
- **Otherwise: the suspension expires at this release by its own
  two-cycle clause.** The cross-field match count returns to the ledger
  as an unadjudicated observation, never a result, and every future
  quotation of it must carry the sensitivity analysis's one-row finding
  (the veto's discrimination rests on the proposition|set exemption).

Also reported, not scored: the real-tags count under the full table
beside the v0.15 scoped-table count of 22, and the agreement between
the blind full table and the v0.15 scoped table on the 44 pairs both
cover — disagreements are listed, not reconciled.

## The population search, closed

Item 2 step 3 asks for one search for an unexamined population. The
corpus has not changed since the v0.15 measurement; the only candidate
(the nine in-field twin groups) was measured at 29% name coverage and
rejected before being run, and that rejection stands. No new population
exists. Recorded here so the step is discharged in writing rather than
silently skipped.
